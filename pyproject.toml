[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-in-the-loop-bo"
version = "0.1.0"
description = "LLM-in-the-Loop Bayesian Optimization and LangGraph HPT backend."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]
